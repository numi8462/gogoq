# gogoQ — 디자인 시스템 (DESIGN.md)

## 디자인 원칙

전체적으로 **미니멀하고 세련된 톤**을 유지하되, 홈화면 하나에서만 확실한 임팩트를
준다. "화려함을 여러 군데 흩뿌리기"가 아니라 "홈의 시그니처 인터랙션 하나에 모든
개성을 몰아준다"는 방향.

- 절제된 배경/여백 위에 포인트 컬러 하나만 강하게 쓴다.
- 장식을 위한 장식은 넣지 않는다. 애니메이션은 정보를 전달하는 곳에만 쓴다
  (다음 모임이 언제인지, 얼마나 남았는지).
- gogoQ의 정체성은 "친구들과의 게임/모임"이므로, 홈 시그니처는 **보드게임의
  징검다리(경로) 위를 이동하는 말(piece)** 컨셉으로 간다.

---

## 컬러 토큰

라이트/뉴트럴 베이스 + 비비드 포인트 컬러 조합. (일반적인 AI 생성 디자인에서 자주
보이는 "따뜻한 크림 배경 + 테라코타" 조합은 피하고, 좀 더 쿨하고 깔끔한 톤으로 감)

| 이름               | 값        | 용도                                                             |
| ------------------ | --------- | ---------------------------------------------------------------- |
| `bg-base`          | `#F7F7F5` | 배경 (살짝 쿨톤 오프화이트, 크림톤 아님)                         |
| `ink`              | `#1C1D22` | 본문 텍스트, 헤드라인                                            |
| `ink-muted`        | `#6B6D76` | 보조 텍스트, 캡션                                                |
| `accent-primary`   | `#5546FF` | 시그니처 포인트 (말/piece, 주요 CTA) — 비비드 인디고             |
| `accent-secondary` | `#FF6B4A` | 오늘/하이라이트 강조 (탄저린 계열, primary와 확실히 구분되는 색) |
| `tile-default`     | `#EAEAE6` | 징검다리 타일 기본 상태                                          |
| `tile-scheduled`   | `#DCE6DD` | 모임이 있는 날의 타일 (세이지 그린, 채도 낮게)                   |
| `border-hairline`  | `#E3E3DF` | 카드/구분선                                                      |

Tailwind에서는 `tailwind.config`의 `theme.extend.colors`에 위 키값 그대로 등록해서
`bg-base`, `text-ink`, `bg-accent-primary` 형태로 쓰는 걸 추천.

### 다크모드 토큰

같은 역할, 다크 배경에서 대비가 나오도록 명도만 조정. Tailwind `dark:` 클래스
전략 기준 (`darkMode: 'class'`) — 토글 버튼으로 켜고 끄는 방식 가정.

| 이름               | 라이트    | 다크      | 용도                                            |
| ------------------ | --------- | --------- | ----------------------------------------------- |
| `bg-base`          | `#F7F7F5` | `#17171B` | 배경                                            |
| `ink`              | `#1C1D22` | `#F2F2F0` | 본문 텍스트                                     |
| `ink-muted`        | `#6B6D76` | `#9A9CA5` | 보조 텍스트                                     |
| `accent-primary`   | `#5546FF` | `#7C6FFF` | 시그니처 포인트 (다크에서 살짝 밝게, 대비 확보) |
| `accent-secondary` | `#FF6B4A` | `#FF8A6B` | 오늘/하이라이트 강조                            |
| `tile-default`     | `#EAEAE6` | `#232328` | 징검다리 타일 기본                              |
| `tile-scheduled`   | `#DCE6DD` | `#213329` | 모임 있는 날 타일                               |
| `border-hairline`  | `#E3E3DF` | `#2A2A30` | 카드/구분선                                     |

**확인 필요**

- [ ] 다크모드 전환 방식: 토글 버튼(수동, localStorage 저장) vs 시스템 설정
      자동 감지(`prefers-color-scheme`) vs 둘 다 지원 — 하나만 만든다면 어느 쪽?

---

## 타이포그래피

| 역할               | 폰트 방향                                                                                              | 용도                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| Display (헤드라인) | 지오메트릭 산세리프, 라운드 터미널이 살짝 있는 것 (예: Pretendard의 굵은 웨이트, 또는 Space Grotesk류) | 홈 히어로 타이틀, 섹션 제목                                                           |
| Body               | 뉴트럴 산세리프 (Pretendard Regular 등 기존 프로젝트에 이미 있는 폰트 유지)                            | 본문, 리스트                                                                          |
| Data/Label         | 모노스페이스 또는 tabular-nums 세팅                                                                    | 징검다리 타일의 요일/날짜 숫자, 시간 표기 — "보드게임 판" 느낌을 숫자 표기에서도 살림 |

**주의**: 기존 프로젝트에 이미 폰트가 지정돼 있다면 그걸 유지하고, 위 방향은 참고만
할 것. 폰트를 새로 바꾸는 건 이 문서의 범위가 아니라 실제 적용 시 별도 확인 필요.

---

## 홈 시그니처: 징검다리 인터랙션 스펙

**컨셉**: 홈 히어로 영역에 요일(월~일) 7개 타일이 징검다리처럼 가로로 배치. 다음
모임이 있는 타일이 `tile-scheduled` 색으로 표시되고, 오늘 타일은
`accent-secondary`로 하이라이트. "다음 모임" 말(piece, 원형 토큰)이 오늘 타일에서
다음 모임 타일까지 통통 튀는 애니메이션으로 이동해서 멈춘다.

**상태**

- 기본: 말은 오늘 타일 위에 정지, 은은한 idle bounce(작게 위아래로 살짝)
- 다음 모임 타일 쪽으로 hop 애니메이션 (타일 하나씩 건너뛰듯, ease-out bounce)
- 타일 클릭/탭 시: 그 날 일정 요약이 작은 팝오버로 뜸
- 모임이 하나도 없는 주: 말은 오늘 타일에 계속 머무름 (빈 상태도 침묵하지 않고
  "이번 주는 비어있어요, 일정을 잡아보세요" 같은 안내로 처리)

**모션 기준**

- `prefers-reduced-motion` 감지 시 hop 애니메이션은 즉시 이동으로 대체 (통통 튀는
  연출 생략)
- idle bounce는 무한 반복이므로 과하지 않게, 거슬리지 않는 주기로

**구현 방식 — 확인 필요**

타일 7개 + 말 1개짜리 정적 구조라서, 사실 **Canvas API 없이 SVG + CSS
transition(또는 Framer Motion)**만으로도 충분히 구현 가능함. Canvas는 보통
파티클 수백 개, 픽셀 단위 드로잉, 게임 루프가 필요할 때 이점이 있는데 이 인터랙션은
그 정도 복잡도는 아님.

다만 이력서에 "Canvas API 활용 경험"을 넣고 싶다는 목적이 있다면 일부러 Canvas로
구현하는 것도 말이 됨 (단순함보다 학습/어필 목적이 우선일 때). 둘 중 어느 쪽을
원하는지 확인 후 진행:

- [ ] SVG + CSS/Framer Motion (더 단순, 유지보수 쉬움, 반응형 대응 쉬움)
- [ ] HTML Canvas (구현 복잡도 ↑, 대신 "Canvas API 경험"을 이력서/면접에서 얘기할
      거리가 생김)

---

## 컴포넌트 톤

- 버튼 문구는 능동태, 시스템 용어 대신 사용자가 하는 행동으로: "일정 추가" O,
  "스케줄 생성 요청" X
- 에러 메시지는 사과하지 않고 원인 + 다음 행동만: "겹치는 일정이 있어요.
  다른 시간을 골라주세요" 형태
- 빈 상태는 방치하지 않고 다음 행동을 유도: "아직 모임이 없어요 → 첫 일정
  추가하기" 버튼 동반

---

## 접근성 최소 기준

- 키보드 포커스 아웃라인 항상 표시 (커스텀 스타일링 하더라도 완전히 없애지 않음)
- 색만으로 상태를 구분하지 않기 (예: 오늘 타일은 색 + 라벨 "오늘" 텍스트 병행)
- 컬러 대비: `ink` on `bg-base` 기준 WCAG AA 이상 확보 (라이트/다크 모드 둘 다 확인)
