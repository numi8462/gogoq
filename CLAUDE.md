# gogoQ — 작업 지침 (CLAUDE.md)

## 프로젝트 개요

친구들끼리 게임/모임 일정을 잡는 웹앱. Supabase Realtime으로 일정 추가/수정이
그룹원 전체에 즉시 반영됨.

**스택**

- Frontend: Next.js 16 (App Router) + TypeScript + TailwindCSS 4
- State: Zustand, TanStack Query v5
- Backend: Supabase (Auth, DB, Realtime)
- Utils: date-fns, lucide-react, nanoid

**목표**: 아래 4개 Phase를 순서대로 추가. 이력서 프로젝트라 "AI를 왜 이렇게 썼는지"를
설명할 수 있는 구조로 만드는 게 기능 자체보다 중요함.

---

## 작업 원칙 (모든 Phase 공통, 반드시 지킬 것)

작업 시작 전:

1. **먼저 기존 코드를 읽어라.** 새 기능을 만들기 전에 관련 폴더 구조, 기존 컴포넌트
   패턴, 상태관리 방식(Zustand store 구조), Supabase 스키마/RLS 정책을 먼저 파악하고
   그 컨벤션을 따른다. 기존 방식과 다르게 짜야 할 이유가 있다면 미리 말한다.
2. **가정하지 말고 질문하라.** 스키마, 인증 흐름, UI 배치 등 여러 해석이 가능한 지점이
   있으면 조용히 하나를 골라 진행하지 말고 옵션을 제시한다. 각 Phase 하단에 "확인 필요"
   항목을 적어뒀으니 그 지점은 특히 멈추고 물어볼 것.
3. **최소 변경.** 요청하지 않은 기능, 나중에 쓸 것 같은 추상화, 요청 안 한
   설정가능성(configurability)은 넣지 않는다. 기존 코드 스타일/포맷팅은 건드리지 않는다.
4. **외과수술처럼.** 리팩터링 필요해 보이는 죽은 코드를 발견해도 삭제하지 말고
   언급만 한다. 내가 만든 변경으로 인해 안 쓰이게 된 import/변수만 정리한다.
5. 각 Phase는 독립적으로 동작 가능해야 함 (Phase 2가 없어도 Phase 1이 굴러가야 함).

---

## Phase 1 — 자연어 일정 추가 챗봇

**목표**: "다음주 금요일 저녁 8시에 축구하자" 같은 자유 텍스트 → 구조화된 일정으로
변환 → 사용자 확인 → Supabase insert (기존 Realtime 파이프라인 그대로 활용).

**범위**

- 채팅 UI (플로팅 위젯 또는 별도 패널 — 확인 필요, 아래 참고)
- LLM 호출은 반드시 서버사이드(Route Handler)에서. API 키 클라이언트 노출 금지.
- LLM은 tool/function calling 형식으로 구조화된 출력만 반환 (자유 텍스트 파싱 금지).
  예: `create_schedule(title, date, time, participants?)`
- LLM이 뽑은 날짜/시간은 **바로 DB에 쓰지 않고 확인 카드로 사용자에게 보여준 뒤
  승인 시에만 insert**. AI가 자율로 확정하지 않는다.
- 상대 날짜("다음주 금요일") 계산은 LLM에게만 맡기지 않고 date-fns로 후처리 검증.

**확인 필요 (진행 전 질문할 것)**

- [ ] LLM 프로바이더: Claude API vs OpenAI — 이력서에 Anthropic API 연동 경험을
      넣고 싶다면 Claude 쪽 추천
- [ ] 채팅 UI 위치: 플로팅 버튼(기존 프로젝트에 비슷한 패턴 있었다면 재사용) vs
      전용 페이지
- [ ] 기준 시간대(timezone) — 서버 기준? 사용자 로컬?

**Out of scope**: 일정 수정/삭제를 챗봇으로 하는 것 (Phase1은 생성만).

---

## Phase 2 — 카카오 로그인 연동

**목표**: Supabase Auth의 Kakao OAuth Provider를 붙여서 카카오 계정으로
로그인 가능하게 함.

**범위**

- Supabase Auth 카카오 Provider 설정 (NextAuth 등 별도 인증 라이브러리 추가하지 않음 —
  이미 Supabase Auth를 쓰고 있으므로 그 안에서 provider만 늘리는 방향)
- 기존 로그인 플로우(있다면 이메일/기타 provider)와 병행 지원

**확인 필요**

- [ ] 현재 인증 방식이 뭔지 (이메일? 이미 소셜 로그인 있는지?) — 기존 auth 코드
      먼저 확인하고 그 패턴에 맞춰 추가
- [ ] 카카오 개발자 앱 등록/리다이렉트 URI는 사용자가 직접 처리할지, 여기서 안내만
      할지

**Out of scope**: 카카오 로그인 외 다른 신규 소셜 로그인 추가.

---

## Phase 3 — 유저 정보 (이름, 주간 여유 시간)

**목표**: 카카오 로그인 연동으로 유저가 식별되니, 그 위에 프로필 정보 저장.
이름, 그리고 "매주 반복되는 여유 시간대"를 받아 Phase 4(최적시간 추천)에서 사용.

**범위**

- `profiles` 테이블 (또는 기존 users 확장) — 이름, 요일별 여유시간 데이터
- 여유시간 데이터 모델: 요일(월~일) × 시간대 블록 형태로 저장 (예: 화요일 19:00-22:00)
- 온보딩 또는 마이페이지에서 입력 UI

**확인 필요**

- [ ] 여유시간 입력 단위: 요일당 하나의 시간 범위만? 여러 개(예: 화요일 오전+저녁)
      허용?
- [ ] 이 정보를 카카오 로그인 시 최초 1회만 받을지, 언제든 마이페이지에서 수정
      가능하게 할지

**Out of scope**: 특정 날짜 단위의 예외 처리(예: "이번주 화요일만 안됨") — 이건
반복 패턴이 아니라 별도 기능이라 이번 Phase에서 제외.

---

## Phase 4 — 참여 멤버 확인 + 최적시간 추천

**목표**: 일정에 참여할 멤버를 확정하고, Phase 3에서 모은 여유시간 데이터로
멤버 전체가 겹치는 시간대를 계산 → AI가 자연어로 추천 문구 생성.

**범위**

- 멤버별 여유시간 교집합 계산은 **일반 로직(TS 함수)으로 처리** — 여기에 LLM을
  쓰지 않는다 (정확도 문제 + 비용 낭비).
- 교집합 계산 결과를 LLM에 넘겨서 "다들 화요일 저녁이 편해 보여요" 같은 자연어
  설명만 생성 (Phase1 LLM 파이프라인 재사용 가능).
- 참여 멤버 확정 UI (일정에 참여/불참 체크)

**확인 필요**

- [ ] 겹치는 시간대가 하나도 없을 때 UX (차선책 제안? 그냥 "없음" 표시?)
- [ ] 참여 확정을 별도 단계로 둘지, 일정 생성과 동시에 처리할지

**Out of scope**: 자동으로 최적 시간에 일정을 확정해버리는 것 (Phase1과 동일하게
AI는 제안만, 확정은 사람이).
